#canvas
.statusbar
    .url {{url}}
    .c1 {{completed}}
    .c2 {{total}}
    .msg
        .wrapper {{message}}
    .status(ng-click='refresh()')
        span.fa.fa-circle-o-notch(ng-class='{\'fa-spin\':waiting==true}')
    .clearfix

