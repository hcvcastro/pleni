article.remove.animate(ng-show='projects.env.view==\'remove\'')
    .title
        h2 Remove Project
        .close
            a.fa.fa-close(ng-click='projects.list()')
    .body
        form(ng-submit='projects.delete()')
            p.large Press the button if you want remove the registry for the project.
            p.offset
                span.hide.loader.fa.fa-spinner.fa-spin
                span.hide.message
            p.offset
                button(type='submit') Remove

