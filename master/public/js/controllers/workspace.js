'use strict';

pleni.controller('WorkspaceController',
    ['$scope','$routeParams','$sessionStorage','$http',
    function($scope,$routeParams,$sessionStorage,$http){
        console.log($routeParams);
    }]
);

